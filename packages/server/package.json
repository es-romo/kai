{
  "name": "@es-romo/relay-server",
  "version": "0.0.0",
  "description": "A tiny WebSocket relay server for listen server architectures",
  "main": "build/index.js",
  "scripts": {
    "start": "node build/src/start.js",
    "build": "tsc",
    "test": "jest"
  },
  "author": "Esteban Romo Esparza",
  "license": "MIT",
  "dependencies": {
    "@fastify/websocket": "^8.2.0",
    "fastify": "^4.24.3"
  },
  "devDependencies": {
    "@types/ws": "^8.5.10",
    "portfinder": "^1.0.32"
  },
  "jest": {
    "testEnvironment": "node",
    "preset": "ts-jest",
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/build"
    ]
  }
}
